export default class LanguageSetting {
    public language: string
    public name: string

    constructor(language: string, name: string) {
        this.language = language
        this.name = name
    }
}